# WILDLIFE MATCH — MASTER BUILD PROMPT

Build the complete site in `starter/` by following every file in this package. The PNG references are binding layouts, not moodboards. `references/01-home-hero-LOCKED.png` is the approved original complex design and must not be simplified.

Before coding, inspect every reference at original size and read all numbered files. Preserve the desktop vertical rail, layered photo mosaics, crossing ivory bands, projecting CTA tabs, editorial serif/sans pairing, forest/ivory/terracotta/gold palette and deliberate overlaps. Do not replace them with a standard header, ordinary split hero, equal cards or generic service template.

Use HTML/CSS/vanilla JS/PHP only. Complete all pages, content, unique local images, config, working forms, cookie consent, legal pages, accessibility and responsive behavior. No phone numbers. Wildlife Match is an independent referral platform, not a contractor. Never invent ratings, testimonials, guarantees, licenses, prices or provider counts.

Follow the build gates and screenshot sizes in `06_VISUAL_QA_CONTRACT.md`. Implement one mapped section at a time, render desktop and mobile screenshots, compare against its reference, patch visual drift, and only then continue. Do not stop at a plan or scaffold. Remove all instruction notes and `CONFIGURE` markers only after replacing them with valid site-owner values or documenting the remaining values in README.
